#include <stdio.h>

/**
 *main - prints the text using puts instead
 *of printf
 *Return: 0 if correct, 1 if not.
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
